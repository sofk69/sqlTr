package tests

/*import (
	_ "sqlTr/internal/domain"
	"testing"
)

func Test_MainTemplate(t *testing.T) {
	testData := map[string][]string{
		// тестовые значения
	}

	expectedData := map[string][]string{
		// ожидаемые значения
	}

	tests := []Test{
		{
			name: "test name",
			verifyResult: func(t *testing.T, p *Test, testName string) {
				// сценарий теста и сравнение результатов с данными из expectedData
			},
		},
	}

	for _, test := range tests {
		p := New()
		t.Run(test.name, func(t *testing.T) {
			test.verifyResult(t, p, test.name)
		})
	}
}*/
